# วิธีทำงานแบบ Loop Engineering

ใช้กติกานี้กับทุกงานใน repository นี้ เว้นแต่ผู้ใช้ระบุเป็นอย่างอื่น
รายละเอียดขั้นตอนเต็ม (Define/Build/Verify/Review/Human gate พร้อมตาราง) อยู่ที่ [docs/LOOP_WORKFLOW.md](docs/LOOP_WORKFLOW.md) — ไฟล์นี้สรุปเฉพาะกติกาที่ต้องยึดเสมอ เพื่อไม่ให้สองไฟล์ drift กัน

## เป้าหมาย

ทำงานให้ถึงผลลัพธ์ที่ตรวจสอบได้ ไม่ใช่เพียงสร้างโค้ดหรือคำอธิบายที่ดูสมเหตุผล

## กติกาหลัก (non-negotiable)

1. หา task spec ใน `docs/tasks/` ก่อนเริ่ม หรือขอข้อมูลที่จำเป็นที่สุดหากยังไม่มี แล้วสรุปผลลัพธ์ เกณฑ์รับงาน ขอบเขต และข้อห้ามอย่างกระชับ
2. ตรวจเทคโนโลยีและคำสั่งตรวจของ repository ก่อนเปลี่ยนไฟล์ ทำการเปลี่ยนแปลงที่เล็กที่สุดซึ่งตอบเกณฑ์รับงาน
3. รันการตรวจที่เกี่ยวข้องจริง (test เฉพาะส่วน, lint/type check, build, smoke test) — ห้ามอ้างว่าผ่านหากไม่ได้รัน ให้เขียน `Not run` พร้อมเหตุผลแทน
4. แก้ได้สูงสุด 3 รอบต่องาน นับเฉพาะรอบที่ลงมือแก้แล้ว หากถูก block หรือเกณฑ์ไม่ชัด ให้หยุดและระบุสิ่งที่ต้องการจากผู้ใช้
5. ไม่ merge, deploy, เปิด PR, ส่งข้อความภายนอก หรือแก้ข้อมูล production โดยไม่มีคำสั่งชัดเจนจากผู้ใช้
6. ก่อนจบ ให้รายงานไฟล์ที่เปลี่ยน, ผลตรวจรายเกณฑ์, ความเสี่ยงที่เหลือ และขั้นตอนถัดไป

## Review loop

เมื่อมี diff หรือ PR ให้ review โดยโฟกัสที่ bug, regression, ความปลอดภัย, ข้อมูลรั่วไหล และการขาด test
ผล review ต้องผูกกับไฟล์/ตำแหน่งและอธิบายผลกระทบ หากไม่มีประเด็น ให้บอกว่าตรวจอะไรแล้วบ้าง
